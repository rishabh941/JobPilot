# Server
server.port=8080

# FastAPI scraper base URL (Spring Boot -> Python scraper)
# Make sure your FastAPI service is listening on this host/port.
jobpilot.scraper.base-url=http://127.0.0.1:8000/scrape-jobs

# ===============================
# MongoDB (recommended, matches MongoRepository)
# ===============================
spring.data.mongodb.uri=mongodb://localhost:27017/jobpilot

# ===============================
# MySQL (JPA) — use only if you switch to JpaRepository
# ===============================
# spring.datasource.url=jdbc:mysql://localhost:3306/jobpilot
# spring.datasource.username=YOUR_DB_USER
# spring.datasource.password=YOUR_DB_PASS
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# ===============================
# Mail (optional)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your_email@gmail.com
spring.mail.password=your_app_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# OpenAI (optional, used by AIService)
# ===============================
openai.api.key=YOUR_OPENAI_KEY

# Logging
logging.level.root=INFO
